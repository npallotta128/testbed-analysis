# Model Performance Capacity (5% ADV)

This summarizes how much capital can be allocated per event using a 5% ADV constraint, grouped by model tiers on the test set.

Generated by `model_performance_capacity.py`.

## Capacity by Tier

- **Top 1% (354 events):**
  - Total Capacity: $4,878,M
  - Average Capacity per Event: $13.78M
  - Median Capacity per Event: $4,488

- **Top 5% (1,770 events):**
  - Total Capacity: $3,491,733M
  - Average Capacity per Event: $1,972.73M
  - Median Capacity per Event: $1,786

- **Top 10% (3,540 events):**
  - Total Capacity: $5,362,198M
  - Average Capacity per Event: $1,514.75M
  - Median Capacity per Event: $1,991

Notes:
- Capacity is computed as `avg_price × avg_volume × 5%`.
- Median capacity is small for many events, indicating illiquidity (long tail of micro-caps), while average is dominated by a few very liquid names.
- Win-rate and forward returns were not available in this test ranking output (NaN), but can be included if `Future_Return` is present for the test set.

## Files
- `model_capacity_by_tier.csv` — machine-readable summary
- `model_performance_capacity.py` — script to recompute

## Next Steps
- Cross-tab capacity with performance: show win rate and forward returns for each tier alongside capacity.
- Filter by minimum capacity threshold (e.g., exclude events with capacity < $50k) to focus on deployable opportunities.
- Repeat analysis for different `TOP_N` values or blocks.
